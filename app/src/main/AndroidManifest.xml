<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testfirebase">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <permission
        android:name="com.androidlearningtutorials.googlemap.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="com.androidlearningtutorials.googlemap.permission.MAPS_RECEIVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ShowActiviQRcode"></activity>
        <activity android:name=".Activities.QRcodeScan" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.NewEdge"
            android:label="@string/title_activity_new_edge" />
        <activity
            android:name=".Activities.NewVertex"
            android:label="@string/title_activity_new_vertex" />
        <activity
            android:name=".Activities.PlaceChooserMap"
            android:label="@string/title_activity_place_chooser_map" />
        <activity android:name=".Activities.NewActivity" />
        <activity android:name=".Activities.ManageActivity" />
        <activity android:name=".Activities.ManageAdmin" />
        <activity
            android:name=".Activities.TestmapInfo"
            android:label="@string/title_activity_testmap_info" />
        <activity android:name=".Activities.testActivity" />
        <activity
            android:name=".Activities.ChooseDestinationActivity"
            android:label="@string/title_activity_choose_destination" />
        <activity android:name=".Activities.ToiletDisplay" />
        <activity android:name=".Activities.LocationDisplay" />
        <activity android:name=".Activities.NewToilet" />
        <activity android:name=".Activities.NewFloor" />
        <activity
            android:name=".Activities.showMap"
            android:label="@string/title_activity_show_map" />
        <activity android:name=".Activities.ManageLocation" />
        <activity android:name=".Activities.LoginAdmin" />
        <activity android:name=".Activities.NewLocation" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>